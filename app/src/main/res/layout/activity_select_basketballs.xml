<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFF8F2"
    android:orientation="vertical"
    android:padding="24dp">

    <!-- Top bar: Return button and Title -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <Button
            android:id="@+id/button_return_home"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:text="←"
            android:textSize="20sp"
            android:textColor="#F57C00"
            android:layout_marginEnd="12dp"
            android:elevation="2dp"/>

        <TextView
            android:id="@+id/balls_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Select Basketball"
            android:textSize="24sp"
            android:textStyle="bold"
            android:textColor="#F57C00"
            android:layout_gravity="center_vertical"/>
    </LinearLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="16dp" />

    <!-- Grid of basketballs -->
    <GridLayout
        android:id="@+id/balls_grid"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:columnCount="3"
        android:useDefaultMargins="true"
        android:alignmentMode="alignMargins"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="16dp" />

    <!-- Selected label -->
    <TextView
        android:id="@+id/selected_ball_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Selected: None"
        android:textSize="16sp"
        android:textColor="#333333"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="12dp"
        android:layout_marginBottom="8dp" />

    <!-- Save button -->
    <Button
        android:id="@+id/save_button"
        android:layout_width="180dp"
        android:layout_height="48dp"
        android:text="Save Selection"
        android:textAllCaps="false"
        android:textColor="#FFFFFF"
        android:backgroundTint="#F57C00"
        android:layout_gravity="center_horizontal"
        android:textSize="16sp"
        android:elevation="2dp"
        android:layout_marginTop="8dp"/>
</LinearLayout>